<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            style="@style/ToolbarStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:contentInsetEnd="0dp"
            app:contentInsetStart="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <TextView
                android:id="@+id/tvBack"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="?attr/selectableItemBackground"
                android:drawableStart="@drawable/icn_back"
                android:drawablePadding="@dimen/_2dp"
                android:fontFamily="@font/sora_semibold"
                android:layout_marginStart="@dimen/_16dp"
                android:gravity="center"
                android:includeFontPadding="false"
                android:text="@string/back"
                android:textColor="@color/hues_blue_celtic_blue"
                android:textSize="@dimen/_14sp" />
        </androidx.appcompat.widget.Toolbar>

        <include
            android:id="@+id/itemCard"
            layout="@layout/item_card"
            android:layout_width="match_parent"
            android:layout_height="@dimen/_196dp"
            android:layout_marginTop="@dimen/_49dp"
            android:layout_marginHorizontal="@dimen/_16dp"
            app:cardHolder="@{@string/abdullah_ghatasheh}"
            app:maskedNumber="@{@string/card_number_back}"
            app:balance="@{@string/card_balance_sample}"
            app:cardColor="@{@color/hues_purple_indigo}"
            app:lightCard="@{false}"
            app:compactHeader="@{false}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/toolbar"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/itemCard"
            app:layout_constraintBottom_toTopOf="@id/btnQRPay">

            <ImageView
                android:id="@+id/imvContactless"
                android:layout_width="@dimen/_24dp"
                android:layout_height="@dimen/_24dp"
                android:layout_gravity="center_horizontal"
                android:contentDescription="@null"
                android:src="@drawable/icn_card_contactless"
                app:tint="@color/neutrals_slate_gray" />

            <TextView
                android:id="@+id/tvMoreDevice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/sora_regular"
                android:layout_gravity="center_horizontal"
                android:includeFontPadding="false"
                android:layout_marginTop="@dimen/_6dp"
                android:text="@string/move_near_a_device_to_pay"
                android:textColor="@color/neutrals_slate_gray"
                android:textSize="@dimen/_16sp" />
        </LinearLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnQRPay"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/_16dp"
            android:layout_marginBottom="@dimen/_33dp"
            android:background="@drawable/bg_wallet_primary_button"
            android:fontFamily="@font/sora_semibold"
            android:includeFontPadding="false"
            android:paddingVertical="@dimen/_12dp"
            android:text="@string/qr_pay"
            android:textAllCaps="false"
            android:textColor="@color/neutrals_white"
            android:textSize="@dimen/_14sp"
            app:backgroundTint="@null"
            app:icon="@drawable/icn_qr_code"
            app:iconGravity="textStart"
            app:iconPadding="@dimen/_4dp"
            app:iconTint="@color/neutrals_white"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
